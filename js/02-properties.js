/*
 * Властивості елемента
 * - Зображення
 * - Текст та textContent
 * https://images.pexels.com/photos/1870376/pexels-photo-1870376.jpeg?auto=compress&cs=tinysrgb&dpr=2&w=480
 */

// const refs = {
//   heroTitle: document.querySelector('.js-hero-title'),
//   heroImg: document.querySelector('.js-hero-image'),
// };

// console.log(refs);

// console.log(refs.heroTitle.textContent);

// refs.heroTitle.textContent = 'Main title!';

// refs.heroImg.alt = 'My cat!';
// refs.heroImg.src =
//   'https://images.pexels.com/photos/1870376/pexels-photo-1870376.jpeg?auto=compress&cs=tinysrgb&dpr=2&w=480';

/*
 * Атрибути
 * - get(ім'я-атрибута)
 * - set(ім'я-атрибута, значення-атрибута)
 * - remove(ім'я-атрибута)
 * - has(ім'я-атрибута)
 */

// console.log(refs.heroImg.getAttribute('alt'));
// console.log(refs.heroImg.alt);

// refs.heroImg.setAttribute('height', 213);
// refs.heroImg.height = 213;

// refs.heroImg.removeAttribute('src');

// console.log(refs.heroImg.hasAttribute('src'));
